import ReactDOM from 'react-dom';
import App from './App';

// Render React-component called "App" inside index.html, within div with id of "root"
ReactDOM.render(
  <App />,
  document.getElementById('root')
);